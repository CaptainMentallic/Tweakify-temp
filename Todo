create a "Profiles" tab
consider replacing most of the tabs with one general "Tweaks" tab
add a "Misc" tab to show credits, feedback (send to discord server or database), and have import/export options
